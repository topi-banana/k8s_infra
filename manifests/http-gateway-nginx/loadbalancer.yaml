apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer
  ports:
    - port: 80
      protocol: TCP
      targetPort: nginx
    - port: 443
      protocol: TCP
      targetPort: nginxs
  selector:
    app: nginx
