apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: nginx
    - name: https
      port: 443
      protocol: TCP
      targetPort: nginxs
  selector:
    app: nginx
