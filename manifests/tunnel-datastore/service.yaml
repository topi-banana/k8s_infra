apiVersion: v1
kind: Service
metadata:
  name: tunnel-datastore-svc
spec:
  selector:
    app: tunnel-datastore
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 122
      targetPort: ssh
