apiVersion: v1
kind: Service
metadata:
  name: tunnel-datastore-svc
spec:
  selector:
    app: tunnel-datastore
  type: LoadBalancer
  ports:
    - name: cifs
      protocol: TCP
      port: 445
      targetPort: cifs

    - name: fileprinters
      protocol: TCP
      port: 139
      targetPort: fileprinters
